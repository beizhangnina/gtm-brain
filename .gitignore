# Secrets — never commit
.env
.env.local
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# 对账器本地状态（可重建，不入库）
state.db
state.db-*
*.jsonl

# 图片衍生品缓存（可重建）
.cache/
derivatives/

# macOS
.DS_Store

# supabase CLI 生成的本地目录
supabase/
